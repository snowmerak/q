# Command terminal smoke (no provider). For structured provider smoke use plan-work-verify with live CLIs.
version: 1
name: structured-smoke
entry: only
limits:
  max_visits: 2
steps:
  only:
    type: command
    command: echo structured-smoke-ok
    terminal: true
edges: []
